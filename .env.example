# Для локального запуска без Docker: make setup && make install && make dev
# При запуске через Docker — переменные задаются в marketplace-stack

# URL бэкенда (без слеша в конце)
# local без Docker: http://localhost:8000/api/v1
# local через nginx: http://localhost/api/v1
VITE_API_URL=http://localhost:8000/api/v1
